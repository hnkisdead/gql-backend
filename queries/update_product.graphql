mutation {
  products {
    update(payload: {id: 12123, name: "Кекерони", category: "Пицца"}) {
      record {
        id
        name
      }
      recordID
      status
      error {
        __typename
        ... on ProductDoesntExistsError {
          message
        }
      }
    }
  }
}
